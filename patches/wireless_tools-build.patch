diff -rup wireless_tools.29-orig/Makefile wireless_tools.29/Makefile
--- wireless_tools.29-orig/Makefile	2014-02-10 21:52:34.301035386 +0200
+++ wireless_tools.29/Makefile	2014-02-10 21:54:59.813039280 +0200
@@ -9,7 +9,7 @@ ifndef PREFIX
 endif
 
 ## Compiler to use (modify this for cross compile).
-CC = gcc
+CC ?= gcc
 ## Other tools you need to modify for cross compile (static lib only).
 AR = ar
 RANLIB = ranlib
@@ -71,10 +71,10 @@ endif
 DYNAMIC_LINK= libiw.so
 
 # Install directories
-INSTALL_DIR= $(PREFIX)/sbin/
+INSTALL_DIR= $(PREFIX)/bin/
 INSTALL_LIB= $(PREFIX)/lib/
-INSTALL_INC= $(PREFIX)/include/
-INSTALL_MAN= $(PREFIX)/man/
+INSTALL_INC= $(PREFIX)/usr/include/
+INSTALL_MAN= $(PREFIX)/usr/share/man/
 
 # Various commands
 RM = rm -f
@@ -102,7 +102,7 @@ ifdef BUILD_WE_ESSENTIAL
 endif
 
 # Other flags
-CFLAGS=-Os -W -Wall -Wstrict-prototypes -Wmissing-prototypes -Wshadow \
+CFLAGS+=-W -Wall -Wstrict-prototypes -Wmissing-prototypes -Wshadow \
 	-Wpointer-arith -Wcast-qual -Winline -I.
 #CFLAGS=-O2 -W -Wall -Wstrict-prototypes -I.
 DEPFLAGS=-MMD
